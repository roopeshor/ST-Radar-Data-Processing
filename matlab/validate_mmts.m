% validates a particular moments with computed ones
% returns the array of
% in/out_mmts = (
%   [M0 Total Power(dBm)],
%   [M1 Mean Doppler (Hz)],
%   [M2 Doppler Spread (Hz)],
%   [SNR (dB)],
%   [Noise Level (dBm)]
%)

function validate_mmts(in_mmts, computed_mmts)
   
end